import { ApiService } from "@/lib/api";

const getCurrentSalesMetrics = async  () => {
	const response = await ApiService(() => fetch(""))
}

const getRecentTransactions = () => {

}

const createNewSalesTransaction = () => {}
